export const options = document.querySelectorAll('.frame .option');
export const status = document.querySelector('.status');
export const showModal = () => status.showModal();
export const closeModal = () => status.close();